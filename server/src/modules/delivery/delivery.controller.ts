import { Request, Response } from 'express';
// TODO: implement delivery controller methods
export const placeholder = (_req: Request, res: Response) =>
  res.json({ module: 'delivery', status: 'stub' });
